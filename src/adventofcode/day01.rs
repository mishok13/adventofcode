pub fn part1(lines: Vec<String>) -> Option<Vec<String>> {
    None
}

#[cfg(test)]
mod tests {
    use super;

    const lines: Vec<String> = vec![];

    #[test]
    fn part1() {
        let result = super::part1(lines);
        assert_eq!(result, Some(vec![]));
    }
}
